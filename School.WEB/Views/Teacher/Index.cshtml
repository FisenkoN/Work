@model School.WEB.ViewModels.Student.Index.IndexViewModel
@using School.WEB.Controllers

@{
    ViewData["Title"] = "Classes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<partial name="_OperationMessage" for="@Model.OperationResult"/>

<h1>It's all student. Please edit only your data</h1>

<table class="table">
    <thead>
    <tr>
        <th>Image</th>
        <th>
            Full Name
        </th>
        <th>Details / Classmates / ClassTeacher</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Students)
    {
        <tr>
            <td>
                <img height="50" src="@item.Image" alt="avatar"/>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                <a asp-action="@nameof(TeacherController.StudentDetails)" asp-route-id="@item.Id">
                    Details
                </a>
                <a asp-action="@nameof(TeacherController.ShowClassmates)" asp-route-id="@item.Id">
                    Show classmates
                </a>
                <a asp-action="@nameof(TeacherController.ShowClassTeacher)" asp-route-id="@item.Id">
                    Show class teacher
                </a>
            </td>
        </tr>
    }
    </tbody>
</table>