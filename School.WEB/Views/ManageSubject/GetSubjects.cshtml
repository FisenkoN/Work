@model School.WEB.ViewModels.ManageSubject.GetSubjects.GetSubjectsViewModel
@using School.WEB.Controllers

@{
    ViewData["Title"] = "GetSubjects";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Varela+Round">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
</head>

<partial name="_OperationMessage" for="@Model.OperationResult"/>

<body>
<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <div class="col-sm-6">
                    <h2>Manage <b>Subjects</b></h2>
                </div>
                <div class="col-sm-6">
                    <a asp-action="@nameof(ManageSubjectController.CreateSubject)" class="btn btn-success">
                        <i class="material-icons">&#xE147;</i> 
                        <span>Add New Subject</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-striped table-hover">
            <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            @foreach(var item in Model.Subjects)
            {<tr>
                 <td>
                     @item.Name
                 </td>
                 <td>
                     <a asp-action="@nameof(ManageSubjectController.EditSubject)" asp-route-id="@item.Id" class="edit">
                         <i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i>
                     </a>
                     <a asp-action="@nameof(ManageSubjectController.DeleteSubject)" asp-route-id="@item.Id" class="delete">
                         <i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i>
                     </a>
                     <a asp-action="@nameof(ManageSubjectController.DetailsSubject)" asp-route-id="@item.Id" class="details">
                         <i class="material-icons" data-toggle="tooltip" title="Details">&#xe417;</i>
                     </a>
                 </td>
             </tr>}
            </tbody>
        </table>
    </div>
</div>
<div>
    <a asp-action="@nameof(AdminController.Index)" asp-controller="Admin"  class="btn btn-warning">
        Back to Admin form
    </a>
</div>
</body>